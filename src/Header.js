import React from 'react'
import {useState} from 'react';
import {AiOutlineShoppingCart} from 'react-icons/ai';
const Header = ({title,length,setCount,count}) => {

//    const [cart,setCart]=useState(0);
  return (
  <div></div>
  )
}

export default Header